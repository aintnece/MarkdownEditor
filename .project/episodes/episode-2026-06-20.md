# Episode: 2026-06-20

## What Was Done

- **Multi-format export**: MD/HTML/PDF/Print via DocumentViewPicker + print API
- **Popup tooltip overhaul**: 9 iterative fixes for bindPopup reactivity
  - inlining all 20 buttons
  - removing @Builder wrappers (breaks reactivity)
  - showInSubWindow:true for clip prevention
  - individual @State timer variables
  - Placement.Top + 400ms delay → 500ms show delay
- **KaTeX integration finalization**: download script (bat + ps1), woff2+woff+ttf all fonts, onInterceptRequest with correct MIME
- **In-place WebView update**: via runJavaScript to preserve scroll position
- **Math block centering**: width:100% on .math-block

## Decisions Made

- bindPopup 提示最终方案：每个按钮独立 `@State isPopupN: boolean = false` + 400ms delay + `showInSubWindow:true`
- KaTeX 不走 CDN：本地 `rawfile/katex/` + `onInterceptRequest` + `$rawfile` 路径处理
- 预览更新走 `runJavaScript('updateContent(...)')` 而非反复 `loadUrl`（保持滚动位置）

## Files Changed

- `.hvigor/` — build cache
- `entry/src/main/ets/components/Toolbar.ets` — 多轮 popup 修复
- `entry/src/main/ets/components/MarkdownPreview.ets` — runJavaScript 更新
- `entry/src/main/ets/components/ExportSheet.ets` — 新增导出选择面板
- `entry/src/main/ets/services/ExportService.ts` — 新增导出服务
- `entry/src/main/ets/pages/EditorPage.ets` — 集成导出
- `entry/src/main/ets/core/HtmlRenderer.ts` — math-block 样式修正
- `entry/src/main/module.json5` — 增加 PRINT 权限
- `download_katex.bat` / `download_katex.ps1` — 新增 KaTeX 下载脚本

## Remaining Work

- 编译告警清理（pushUrl → pushPage, getContext → getUIContext 等弃用 API 替换）
- 代码重构（Toolbar.ets 重复逻辑抽取）

## Known State

- 编译 0 error，仅弃用 API warnings
- GitHub main 分支 `e046580` 为最新提交
- 数学公式渲染已通过 onInterceptRequest + 本地 KaTeX 字体完整解决
